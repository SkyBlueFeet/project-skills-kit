# CSS 代码规范（工程执行版）

## 1. 架构

- 使用稳定命名体系（建议 BEM 或等价约束）。
- 主题变量统一放在 `:root` 或主题文件，禁止散落硬编码色值。

## 2. 可维护性

- 单个选择器层级不超过 3 层，避免过深嵌套。
- 避免 `!important` 常态化；仅在必要兼容场景使用并注释原因。
- 按“布局 -> 盒模型 -> 视觉 -> 动效”组织声明顺序。

## 3. 响应式与兼容

- 采用移动优先断点策略。
- 关键布局需覆盖常见桌面与移动宽度。

## 4. 检查建议

- 建议命令：`stylelint "**/*.css"`。
